 //DIFERENCIAS ENTRE FUNCION Y METODO

 
const numero1 = 20;
const numero2 = "20";

//FUNCIÓN -> se invoca una funcion predefinida y se le puede mandar argumentos.
console.log(parseInt(numero2)); 

//MÉTODO -> si antes de la funcion esta el nombre de una variable o un array etc. se le llama Método
console.log(numero1.toString()); 